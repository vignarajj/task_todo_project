name: task_todo_project
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.3.6
  appwrite: ^13.1.1
  share_plus: ^7.0.2
  intl: ^0.18.0
  uuid: ^3.0.7
  envied: ^1.3.1
  app_links: ^6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  envied_generator: ^1.3.1

dependency_overrides:
  flutter_web_auth_2: ^4.0.0

flutter:
  uses-material-design: true
